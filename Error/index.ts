import type { InfiniteLoopError } from '../InfiniteLoopError'

/**
 * An error which can be encountered while interpreting a parsed document.
 */
export type Error = InfiniteLoopError

/* c8 ignore next */
