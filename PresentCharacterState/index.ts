/**
 * A character is currently visible, and is not animating.
 */
export interface PresentCharacterState {
  /**
   * Identifies the type of character state.
   */
  readonly type: 'present'
}

/* c8 ignore next */
