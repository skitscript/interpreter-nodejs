/**
 * A character is currently not visible.
 */
export interface NotPresentCharacterState {
  /**
   * Identifies the type of character state.
   */
  readonly type: 'notPresent'
}

/* c8 ignore next */
